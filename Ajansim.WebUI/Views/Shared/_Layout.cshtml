<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ajans Adı | Kreatif Dijital Çözümler</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet">
    <!-- HEAD kısmına -->
    <link href="https://unpkg.com/aos@2.3.4/dist/aos.css" rel="stylesheet">
    <style>
        .fade {
            opacity: 0;
            transition: opacity 1s ease-in-out;
        }

            .fade.active {
                opacity: 1;
            }
    </style>
</head>
<body class="bg-gray-100 text-gray-800">
    <!-- Navbar -->
    <nav class="bg-dark shadow-md sticky top-0 z-50">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between h-16 items-center">

                <!-- Logo -->
                <div class="flex-shrink-0">
                    <a href="/" class="text-2xl font-bold text-blue-600">Fraoula</a>
                </div>

                <!-- Menü -->
                <div class="hidden md:flex space-x-6">
                    <a href="/" class="text-gray-500 hover:text-blue-600 transition">Anasayfa</a>
                    <a href="/blog" class="text-gray-500 hover:text-blue-600 transition">Blog</a>
                    <a href="/hakkimizda" class="text-gray-500 hover:text-blue-600 transition">Hakkımızda</a>
                    <a href="/iletisim" class="text-gray-500 hover:text-blue-600 transition">İletişim</a>
                </div>

                <!-- Mobil Menü Butonu -->
                <div class="md:hidden">
                    <button id="mobile-menu-button" class="text-gray-500 hover:text-blue-600 focus:outline-none">
                        <svg class="h-6 w-6" fill="none" stroke="currentColor" stroke-width="2"
                             viewBox="0 0 24 24" stroke-linecap="round" stroke-linejoin="round">
                            <path d="M4 6h16M4 12h16M4 18h16" />
                        </svg>
                    </button>
                </div>
            </div>
        </div>

        <!-- Mobil Menü -->
        <div id="mobile-menu" class="md:hidden hidden px-4 pb-4">
            <a href="/" class="block py-2 text-gray-700 hover:text-blue-600">Anasayfa</a>
            <a href="/blog" class="block py-2 text-gray-700 hover:text-blue-600">Blog</a>
            <a href="/hakkimizda" class="block py-2 text-gray-700 hover:text-blue-600">Hakkımızda</a>
            <a href="/iletisim" class="block py-2 text-gray-700 hover:text-blue-600">İletişim</a>
        </div>
    </nav>

    @RenderBody()

    <!-- Footer -->
    <footer class="bg-dark text-white text-center py-4 mt-5">
        <p class="mb-0">© 2025 AjansAdı. Tüm hakları saklıdır.</p>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <!-- BODY sonunda, script'lerden önce -->
    <script src="https://unpkg.com/aos@2.3.4/dist/aos.js"></script>
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        AOS.init({
          duration: 800,
          easing: 'ease-in-out',
          once: true
        });
    </script>
    @*     <script>
        window.addEventListener('scroll', function () {
          const navbar = document.querySelector('.navbar');
          if (window.scrollY > 50) {
            navbar.classList.add('bg-dark', 'shadow');
          } else {
            navbar.classList.remove('bg-dark', 'shadow');
          }
        });
    </script> *@
    <!-- Navbar için küçük bir JS -->
    <script>
        const btn = document.getElementById('mobile-menu-button');
        const menu = document.getElementById('mobile-menu');
        btn.addEventListener('click', () => {
          menu.classList.toggle('hidden');
        });
    </script>
    <!-- SLIDER SCRIPT -->
    <script>
        let index = 0;
        const slider = document.getElementById("sliderImage");

        setInterval(() => {
          index = (index + 1) % images.length;
          slider.classList.remove("active");
          setTimeout(() => {
            slider.src = images[index];
            slider.classList.add("active");
          }, 200);
        }, 4000);
    </script>
    <script>
        let index = 0;
        const slider = document.getElementById("sliderImage");

        setInterval(() => {
          index = (index + 1) % images.length;
          slider.classList.remove("active");
          setTimeout(() => {
            slider.src = images[index];
            slider.classList.add("active");
          }, 200);
        }, 4000);
    </script>
</body>
</html>